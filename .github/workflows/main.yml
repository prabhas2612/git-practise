name: test-if
on: push
jobs:
   test:
     runs-on: ubuntu-latest
     steps:
        - run: github.actor
        - name: run if or
          if: github.actor == 'prabhas'  ||  github.actor == 'vasanth'
          run: echo "triggered"
