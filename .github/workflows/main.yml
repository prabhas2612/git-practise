
name: test-env
on: workflow_dispatch
env:
  APP_ENV: "dev"
jobs:
  test-env:
    env:
      APP_ENV: "test"
    runs-on: ubuntu-latest
    steps:
      - name: connecting to env
        run: echo "app connecting to $APP_ENV"
